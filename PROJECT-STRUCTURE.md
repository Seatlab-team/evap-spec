# Project Structure (MVP)

MVP repositories:
- evap-spec: protocol + docs
- evap-resolver: reference resolver gateway
- evap-website: website

Future split (optional):
- evap-js, evap-py, eaml-rules, registry, vault, playground
